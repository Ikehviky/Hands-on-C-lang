#include<stdio.h>
#include<string.h>

int main()
{
    char ch;
    char s1[30];
    int i, len;

    printf("Enter your string for reversing: \n");
    fgets(s1, sizeof(s1), stdin);

    

    // len = strlen(s1);

    // for(i = 0; i < len/2; i++)
    // {
    //     ch = s1[i];
    //     s1[i] = s1[len-1-i];
    //     s1[len-1-i] = ch;
    // }

    //     printf("%s\n", s1);

    
}